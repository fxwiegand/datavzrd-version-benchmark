# datavzrd-version-benchmark

A Snakemake workflow that benchmarks datavzrd 2.72.0 against 2.72.1 across dataset row and column counts and renders the runtime and peak memory differences as a datavzrd report.
